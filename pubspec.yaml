name: play_space
description: "PlaySpace — Book, check-in, and manage your family's children's gym and indoor play experience."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^14.8.1
  google_fonts: ^6.2.1
  font_awesome_flutter: ^11.0.0
  velocity_x: ^4.3.1
  table_calendar: ^3.2.0
  qr_flutter: ^4.1.0
  signature: ^5.5.0
  intl: ^0.20.3
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
